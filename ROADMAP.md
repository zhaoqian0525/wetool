# 微坞 WeWoo 发展路线图（2026-08 修订版）

> 本文档 = 产品战略 + 功能计划 + 技术演进 + Bug/体验问题清单 + 近期任务拆解。
> 维护规范：每个版本发布前更新 version.json / CHANGELOG.md / README badge，并在本文件勾选完成项。

---

## 一、当前状态（v1.15.6，2026-08-13）

已上线：注册登录（可填昵称）、工具发布/编辑/改编、iframe 沙盒预览、全屏模式、点赞/收藏、评论/评分、使用历史、最近使用、我的工具、分类筛选（含小游戏）、搜索（服务端 ilike + 前端防抖）、浏览量统计、下载型工具、三级可见性、PWA、SEO、封面系统、新手教程、微信内嵌浏览器兼容、移动端创作（编辑/全屏预览切换）、内置 AI 直接生成（DeepSeek 流式）、对话式创作（多轮修改 + 多版本切换 + 生成自动填入编辑器 + 快捷场景）、改编对话、权限安抚反馈、AI 成本控制（上下文压缩 / 限流 / 余额保护 / 用量日志）、密钥泄漏修复、AI 模型钉死 flash 低价档、关闭思考模式防正文截断、创作页三 tab（对话/代码/预览）+ 桌面三栏、改编对话隔离、外部提示词折叠、安装到主屏幕提示、预览 tab 直通全屏、AI 输出/输入长度限制放松（长工具完整生成）、教程入口补全（创作页顶部教程按钮 / 对话空状态引导 / 登录后教程横幅重显）、12 秒误报「语法错误」根治（READY 确认按 iframe 实例记忆 + PING 补收 + onLoad 兜底，重试真正重建 iframe）、创作页顶部按钮样式统一（教程图标+文字，示例/教程统一白底靛蓝描边次级样式）。 封面截图生产修复（v1.15.6：@sparticuz/chromium externalize + bin/fonts 打进函数包，生产自动截图 500 修复并附错误 detail；新增服务端渐变封面接口 /api/cover/gradient，无头浏览器原生渲染，修复 iOS 渐变封面无 emoji；Vercel 无头环境注入 NotoSansSC + NotoColorEmoji 字体（FONTCONFIG_PATH 指向 /tmp/fonts 目录 + 解压后重写 fonts.conf），用户 HTML 中文/emoji 正常渲染）。 P2 设计系统深化第一批（token 完整落地：圆角 12/20/35/文字层级/背景层级/页面宽度；语义工具类 panel/btn-*/input-base；组件化 Modal 统一 6 处弹窗 + Badge 统一徽章）。 暗色模式（v1.12.1：深/浅主题切换，跟随系统 + localStorage 记忆 + 首帧防闪烁，浅色模式零改动，全站 9 类页面暗色适配，无浅色残留）。 UI 形状统一（v1.12.2：修复 rounded-lg 被 token 误改 35px 的圆角体系 bug，create 页按钮/首页排序/登录按钮/封面图标格/卡片圆角全面统一）。 工具卡片封面统一（v1.12.3：个人主页/首页/相关推荐卡片图标全部改用工具封面图，无封面纯渐变占位，移除 emoji 图标）。 最近使用封面与手机端修复（v1.12.4：recent-tools 接口补 coverUrl；修复 String(undefined) 误判与无封面工具破图兜底（统一渐变占位，仅内置工具保留公共封面）；token 过期 401 或网络失败保留最近使用缓存，authedFetch 自动刷新 session 重试）。 提速与收尾（v1.13.0：18 个内置封面转 WebP（体积 -72%）并切换引用；详情数据缓存（fetchToolById 写缓存 + 详情页预填秒开，24h TTL）；ROADMAP 技术债状态整理勾选）。 个人头像与分享优化（v1.13.1：头像上传更换（压缩 512px/WebP → Storage → user_metadata），导航栏/用户页统一 Avatar 组件；工具页 OG 分享图改用工具封面 + 描述带作者，用户主页分享 metadata）。 评论系统完善 + 创作新对话 + 用户页提速（v1.14.0：评论最新/最热排序、回复（@昵称 + 嵌套 + 作者标记 + 头像）、点赞数展示；底部创建入口 ?new=1 开启全新对话；用户主页工具列表本地缓存秒开；更换头像按钮修复为圆形；修复网络抖动误弹「工具未找到」（网络失败与确认不存在区分 + 详情缓存兜底 + 可重试失败视图））。 安全加固（v1.15.0：封面截图沙箱修复（T4 快照机制：沙盒运行 → DOM 快照回传 → 无脚本 iframe + html2canvas 截图，用户代码不再接触 allow-same-origin）、修复封面构建 head 注入 bug（`<head><style>` 紧凑结构时 CSP/重置样式被塞进 style 标签导致样式失效且 CSP 未生效）、搜索参数化转义、可见性落库、AI 限流跨实例化（Supabase RPC 计数，未部署自动回退内存 Map + x-forwarded-for 取末段）、lint 修复（eslint .）；配套 supabase_fix_rls_all.sql 需在 Dashboard 执行：全表 RLS + Storage owner 约束 + ai_rate_limit 表/RPC）。 登录态闪现修复（v1.15.1：PWA/standalone 打开时登录按钮闪一下消失——工具页点赞区/评价区/全屏横幅/首页 standalone 横幅在 authLoading 期间显示骨架占位，登录态就绪后渲染真实内容）。 PWA 主屏快捷方式地址修复（v1.15.2：iOS「添加到主屏幕」被存成官网首页——工具 manifest URL 版本化 ?v=2 + 响应 no-store，强制 iOS 重新抓取正确的工具 start_url，老设备需删除旧快捷方式并清除网站数据后重加）。 封面上传策略修复（v1.15.2：Storage 策略 foldername()[2] 对 public/<toolId>.png 恒为 NULL 锁死所有新封面/头像上传，改用 filename() 解析；需执行 supabase_fix_rls_storage_v2.sql）。 自动截图卡死修复（v1.15.3：html2canvas 截图阶段加 15s 超时兜底——iOS Safari 下 html2canvas 可能永不返回导致发布/更换封面一直卡在「正在生成封面」，超时后自动走默认渐变封面，发布与更换封面流程均不再卡死）。 封面链路彻底防挂起（v1.15.4：给 canvas.toBlob 补 8s 超时、dataURL 转 Blob 补 8s 超时、Supabase 封面上传补 25s 超时——v1.15.3 后 iPhone Safari 仍卡死，根因是最后一步 toBlob 永不回调且无超时保护，现任一步骤挂起都会自动走渐变封面兜底；同时剥离 srcdoc 开头的 <!DOCTYPE 兼容 Safari）。

**2026-08-12 双 AI 安全审查（已核验采纳）**：外部 AI 对平台做全量代码走读 + 线上只读探测，以下问题已人工核验属实并纳入计划：

- **P0-1 RLS 大面积未生效（已实测）**：anon key 可直读 tools / likes / reviews / user_pinned_tools / tool_state / tool_usage_history / tool_drafts / tool_recent 全部表（tools 返回含 author_id，likes 返回含 user_id）。现有 supabase_fix_rls.sql 仅覆盖 tools/favorites/reviews，其余表线上从未开 RLS。
- **P0-2 captureCover 沙箱带 allow-same-origin**（src/lib/cover.ts:35-37）：封面截图 iframe 以同源权限加载用户 HTML，与安全模型矛盾（可读写父页 localStorage/token）。修复需技术验证（blob URL 或服务端截图），不可贸然改（会破坏封面功能）。
- **P1 lint 失效**：package.json 仍是 "lint": "next lint"，Next 16 已移除该命令（.eslintrc.json 遗留、eslint.config.mjs 未接入）。
- **P1 可见性切换是假功能**：handleChangeVisibility 只改前端 state，未落库 Supabase（刷新失效）。
- **P1 搜索注入面**：/api/tools/search 把用户 q 直接拼进 PostgREST .or() 字符串，逗号/括号未转义。
- **P1 Storage 授权过宽**：cover_auth_update 允许任意登录用户覆盖/删除 tool-covers 桶任意文件，无 owner 校验。
- **P1 限流仅内存 Map**：checkRateLimit 模块级 Map，Vercel 多实例/冷启动可绕过；x-forwarded-for 首段可伪造。
- **P2**：浏览量造假（内置工具只写本地不加库）、AI 用量无持久化/无告警、首页 fetchTools 无分页、发布无幂等、anon key 硬编码 3 处、零测试零 CI、robots 与 sitemap 矛盾（robots 禁 /create /auth 但 sitemap 收录）、无内容审核、out/ 残留目录。
- **勘误确认**：Vercel 函数超时非 10s（Fluid Compute 默认 300s）；src/proxy.ts 是 Next 16 中间件改名（非死代码，安全头生效）。

已知技术债 / 风险（本次代码审查发现）：

| # | 级别 | 问题 | 位置 |
|---|------|------|------|
| B1 | P0 安全 | state / history API 只信任客户端传入的 userId，无登录鉴权，任何人可读写删除任意用户数据（IDOR）（✅ v1.5.0 已修复：改为服务端 session 鉴权） | src/app/api/tools/[id]/state/route.ts、history/route.ts |
| B2 | P1 | 使用记录抽屉只读 localStorage，与 Supabase tool_usage_history 不同步，跨设备丢失（✅ v1.5.0 已修复：改调 /api/tools/[id]/history，localStorage 仅离线缓存） | src/components/ToolHistoryDrawer.tsx |
| B3 | P1 | 他人主页：头像取当前登录用户邮箱首字母；标题显示 UUID 前 8 位而非用户名（✅ v1.9.10 已修复：注册可填昵称，主页/发布/评论统一展示昵称） | src/app/user/[id]/page.tsx |
| B4 | P1 | 全屏 iframe 状态注入时序不稳（querySelectorAll('iframe')[0] 可能拿错），全屏后状态可能丢失（✅ v1.6.0 已修复：全屏复用同一 iframe + ref 指向当前实例） | src/app/tool/[id]/page.tsx |
| B5 | P1 性能 | 首页 fetchTools select('*') 把每个工具完整 HTML 代码全量拉下来，工具越多越慢（✅ v1.5.0 已修复：列表只取卡片字段，详情才拉 code） | src/lib/data.ts |
| B6 | P2 | 数据层 3300+ 行单文件，MOCK_TOOLS 代码 + 业务函数混在一起，难维护 | src/lib/data.ts |
| B7 | P2 | 分类 emoji 映射在 3 个页面重复定义（✅ v1.9.10 已修复：统一到 lib/constants） | page.tsx / tool/[id]/page.tsx / user/[id]/page.tsx |
| B8 | P2 | 搜索只对已加载工具前端过滤，无服务端/DB 搜索，工具量大后不可用（✅ 已上线：服务端 ilike 搜索 /api/tools/search + 前端防抖） | src/app/page.tsx |
| B9 | P2 | 浏览量每次打开 +1（刷新即 +1），可能虚高 | src/lib/data.ts incrementToolView |
| B10 | P2 | 创作页停留 30s 自动展开 AI 提示词，属打扰性设计（✅ 已修复：v1.8.6 起默认折叠，仅点击展开） | src/app/create/page.tsx |
| B11 | P2 | 工具页同时注册 useToolStorage + 页面自带 message 监听，同一消息双重处理，行为不一致 | src/app/tool/[id]/page.tsx |
| B12 | P2 | 未登录（游客）无法使用任何状态持久化，纪念日类工具游客一刷新就清空 | 全站 |
| B13 | P2 | create 页 'useToolStorage("preview")' 将编辑器预览状态写入 wewoo-db-preview 命名空间，与正式工具状态隔离但无清理策略 | src/app/create/page.tsx |
| B14 | P0 安全 | RLS 大面积未生效：tools/likes/reviews/user_pinned_tools/tool_state/tool_usage_history/tool_drafts/tool_recent 可匿名直读（含 user_id/author_id），supabase_fix_rls.sql 仅覆盖 tools/favorites/reviews，supabase_view_count.sql 线上未执行 | supabase_fix_rls.sql / supabase_view_count.sql |
| B15 | P1 | 工具可见性切换是假功能：handleChangeVisibility 只改前端 state，Supabase visibility 未落库，刷新失效 | src/app/tool/[id]/page.tsx |
| B16 | P1 安全 | 搜索 route 把用户 q 直接拼进 PostgREST .or() 字符串，逗号/括号未转义，存在过滤注入面 | src/app/api/tools/search/route.ts |
| B17 | P1 安全 | Storage cover_auth_update 允许任意登录用户覆盖/删除 tool-covers 桶任意文件，无 owner 校验 | supabase_fix_storage.sql |
| B18 | P1 | lint 失效：package.json 仍是 "next lint"（Next 16 已移除该命令），eslint.config.mjs 未接入 | package.json |

---

## 二、宏观战略

### 2.1 产品定位

**微坞 = "AI 时代的工具箱 + 朋友圈"**：让不会编程的人（包括父母辈）也能一键使用、分享 AI 生成的小工具；让手机成为主要使用场景。

### 2.2 三大阶段

| 阶段 | 时间 | 目标 |
|------|------|------|
| 阶段 1 稳定期 | 现在 → 约 1-2 月 | 修完 P0/P1 问题，上线记忆机制，体验打磨，建立审核底线 |
| 阶段 2 生态期 | 1-3 月 | 评论/榜单/管理后台/审核体系，内置 AI 生成，创作者激励机制 |
| 阶段 3 开放期 | 3-6 月+ | 能力契约 + 分级权限 + 外部模式 + 开放平台 API，成为"人人都能用的工具分发平台" |

### 2.3 宏观体验 8 大提升方向

1. **记忆连续性（最高优先）**：状态冻结 / 墓碑机制，任何工具（含全屏）退出后回来原样恢复 → 纪念日记录器不再清空。
2. **个性化首页**：最近使用、我的工具、收藏、智能推荐（按分类/历史）替代统一大广场。
3. **免登录 + 游客体验**：游客本地墓碑（localStorage/IndexedDB），登录后自动合并上云；降低使用门槛。
4. **一句话创建**：内置 AI 对话式生成，不再需要"复制提示词去外部 AI"。
5. **场景模板库**：纪念日、记账、习惯打卡、倒计时、抽签等高频场景一键生成。
6. **信任体系**：审核、举报、作者等级、工具质量分，让好工具浮上来。
7. **适老化**：大字模式、语音搜索、极简引导、长辈友好配色，覆盖父母辈。
8. **微信触达**：分享卡片、复制文案、微信内打开引导、未来小程序/公众号承接。

### 2.4 技术演进路径：内置 AI + 权限体系

权限分级（核心设计）：

| 级别 | 名称 | 能力 | 说明 |
|------|------|------|------|
| L0 | 基础沙盒 | 纯前端、无网络、无存储 | 现状（sandbox=allow-scripts + CSP + shim） |
| L0+ | 权限透明反馈 | 工具能/不能做什么，对用户与作者清晰展示 | v2.0（major，涉及沙盒/注入内核） |
| L1 | 记忆沙盒 | + 持久状态读写（经统一 API） | ⏸ 待定：用户持保留态度，暂缓开放 |
| L2 | 受控网络代理 | + 白名单域名 fetch（经 /api/proxy） | 阶段 2，限流+审计 |
| L3 | 外部模式 | 新标签页运行完整权限 HTML | 用户明确同意 + 风险提示，不保存使用记录 |

演进顺序：**AI 内置生成（便捷）→ 权限透明反馈（让用户/作者清楚边界）→（待定）统一工具 API/SDK（wewoo.* 能力契约）→ 分级权限授权 → 开放平台**。

> 2026-08-09 决策：**wewoo.* 沙盒能力开放（memory / sound / notify / fetch / ai）暂缓，用户持保留态度**。优先做「权限透明反馈」（v2.0 major，涉及沙盒/注入内核）——让用户和作者清楚知道每个工具能做什么、不能做什么；API 开放等产品形态更清晰或出现真实需求再评估。若未来开放，必须满足兼容性红线（见第三章 v2.0 拆解）。

- P1：AI 生成引擎（OpenAI 兼容、可插拔、流式输出、系统提示词强制白名单：纯前端/自包含/沙盒合规）。
- P2：能力契约 + AST 静态检测（发布时自动识别代码请求了哪些权限，声明与实际一致才可发布）。
- P3：/api/proxy 白名单代理（限流、审计、缓存）。
- P4：开放平台（第三方开发者、SDK 文档、应用市场化）。

**Python 兼容性结论：现阶段不做。** Pyodide 体积大、手机内存吃不消、生态受限；服务端执行有沙箱逃逸风险。内置 AI 后，用户直接说需求 → AI 翻译成合规 JS，用户无感知。远期（P3）可评估 Pyodide 子集（如只支持纯计算库）。

**白盒能力现状（2026-08-12 审查确认）**：L0 沙盒内已可用——云记忆 KV（__wewoo.save/load/remove/list/clear）、表单草稿/状态墓碑（saveDraft/loadDraft/saveState/loadState + 输入自动监听）、使用记录（recordAction/getHistory）、localStorage shim 自动同步父页面、Canvas/Canvas 动画、DOM/CSS、定时器、WebAudio 合成音效、FileReader 读本地文件、正则/计算/算法。**结论：90% 的「计算 + 记录 + 娱乐」类工具已可覆盖**，主要缺口是三类体验：复制结果、导出文件、系统级提醒/输入。

**高价值应用方向（现有权限即可做，供场景模板库 / 内置工具扩充参考）**：

| 类别 | 示例 |
|------|------|
| 实用型 | 家庭账本、房贷/复利/提前还款计算器、纪念日与倒数日（配记忆自动恢复）、药箱用药记录、体重/血糖趋势图（Canvas 折线）、番茄钟/专注计时（WebAudio 提示音可完全离线）、白噪音发生器、决策转盘/抽签、随机点名/分组、家庭事务轮值表、身份证/银行卡校验、退休倒计时、时区换算钟 |
| 教育型 | 口算/古诗词/单词小测（已有，可继续扩展）、元素周期表、乐理练习（节拍器/音阶跟练/视唱练耳，WebAudio 零成本）、打字测速、错题本（云记忆）、历史年代轴、地理常识自测 |
| 工程/效率型 | 单位换算/色环电阻/欧姆定律、正则测试器、JSON/CSV 格式化、Markdown 预览、文本 diff、表格转 Markdown、敏感词自查、图片工具全家桶（压缩/九宫格/水印/滤镜/表情包/二维码，Canvas + 本地文件）、提示词模板库、AI 输出格式转换 |
| 有趣型（小游戏） | 2048、扫雷、数独、贪吃蛇、俄罗斯方块、华容道、记忆翻牌、五子棋人机、打字竞技、节奏打击游戏（WebAudio 音乐）、Canvas 物理小游戏、像素画编辑器 |

**零风险能力（按「解锁价值 ÷ 风险」排序，建议并入 v2.0 权限透明反馈版本的第一批，不单独抢 minor）**：

| 优先级 | 能力 | 解锁场景 | 落地方式 |
|--------|------|----------|----------|
| P0 | 剪贴板写入 | 一键复制结果/文案/密码（现在沙盒内不可靠） | iframe 加 allow="clipboard-write" 或父页面代理；**永不开放剪贴板读取** |
| P0 | 文件导出（wewoo.download） | 图片保存、导出 CSV/报告/日历文件——图片工具和办公工具刚需 | 复用父页面下载机制扩展为代理 API |
| P0 | Web Worker | 大数据计算、图片处理、物理游戏不卡 UI | CSP script-src 加 blob:（一行改动，纯性能提升） |
| P0 | 语音朗读 + 音效 | 适老化朗读、游戏音效、学习跟读 | WebAudio 已可用；补 speechSynthesis 验证 + 写进 AI 生成提示词 |
| P1 | 白名单联网代理（wewoo.fetch → /api/proxy，即 L2） | 汇率/天气/词典/翻译——工具从「单机」变「应用」的最大杠杆 | 仅 GET + 域名白名单 + 限流 + 审计 + 缓存；不代持 Cookie |
| P1 | 通知（wewoo.notify → 父页面 Notification/Web Push） | 喝水/用药/番茄钟/纪念日提醒类工具变「活」的 | 用户逐工具授权、可撤销 |
| P1 | 只读用户信息（wewoo.user） | 个性化问候、排行榜、「我的数据」 | 只暴露昵称/头像，不暴露邮箱/ID |
| P1 | 分享卡片（wewoo.share） | 工具结果一键生成分享文案/图片，配合微信生态 | 父页面拼装分享 |
| P2 | 摄像头/麦克风/定位（allow="camera/microphone/geolocation"） | 扫码查件、语音输入、天气打卡 | 必须用户手势触发 + 逐次授权 |
| P2 | 全屏 + 指针锁定（allow="fullscreen; pointer-lock"） | 游戏沉浸体验 | 仅游戏类工具声明 |
| P3 | 外部模式（L3） | 完整权限复杂工具，新标签页运行 | 显式同意 + 风险提示 |

**安全红线（架构级约束，写死不可破）**：
1. **永不开放 allow-same-origin**——这是隔离主站 Cookie/localStorage 的生命线。
2. **永不开放任意 URL 联网**——只走白名单代理，可审计、可撤销。
3. **不开放原生 cookie/IndexedDB**——统一走平台 KV，方便数据保护与「注销即删」。
4. **权限三件套**：工具声明 → 发布时 AST 静态扫描核对 → 用户首次使用时弹授权、可随时撤销。分级审核：L0 免审、L1 自动 + 抽检、L2 人工审核、L3 强提示。

**权限测试场（建议随 v2.0 一起做）**：做一个「权限测试场」内置工具，让用户直观看到每个权限是什么、有什么用，反而能降低对开放权限的戒心，也是很好的社区教育素材。

---

## 三、近期详细拆解（4 周，按版本发布）

### v1.5.0 稳定与安全（✅ 已完成 2026-08-06）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| 1.1 状态/历史 API 鉴权 ✅ | state、history 两个 route 改为读取 Supabase session（getUser），用 session.user.id 代替客户端 userId；前端传参改为不传/仅作兼容 | 用他人 userId 无法读取；curl 验证 401/403 |
| 1.2 使用记录跨设备同步 ✅ | ToolHistoryDrawer 改为调用 /api/tools/[id]/history GET/DELETE；localStorage 仅作离线缓存；工具页"打开"记录保留 DB 写入 | 手机、电脑看到同一份记录；删除/清空生效 |
| 1.3 用户主页资料 ✅ | 新增 users 资料表或复用 auth.users 元数据：头像、昵称、简介、注册时间；他人主页显示对方资料而非当前用户 | 他人主页头像/昵称正确 |
| 1.4 全屏状态注入修复 ✅ | 全屏 iframe 与内嵌 iframe 共用同一状态处理器（ref 指向当前激活的 iframe，而非 querySelectorAll[0]）；进入全屏时重新注入快照 | 内嵌→全屏→退出，状态不丢 |
| 1.5 首页列表瘦身 ✅ | fetchTools 拆分为 fetchToolCards（select 列表字段，不含 code）+ fetchToolById（含 code）；缓存结构同步升级 | 首页网络体积下降 80%+（工具多时更明显） |
| 1.6 版本规范落实 ✅ | version.json / CHANGELOG / README badge / ROADMAP 勾选 | 发布后主页底部版本号正确 |

### v1.6.0 记忆机制（墓碑/状态冻结） ✅ 已完成（2026-08-06）

核心方案（已与用户对齐方向，透明持久化）：

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 2.1 sandbox shim 升级 | src/lib/sandbox.ts 中内存 localStorage mock 升级为"内存 + 自动同步"：读取时父页注入快照，写入时防抖（800ms）通过 postMessage 同步 | 工具内 setItem 后刷新页面值还在 |
| ✅ 2.2 状态注入时序 | 父页收到 WEWOO_READY 后注入快照；增加 ready 确认/超时重试；全屏 iframe 接入同一注入逻辑 | 手机内嵌/全屏都能恢复状态 |
| ✅ 2.3 同步到 Supabase | setItem 防抖 → POST /api/tools/[id]/state；同步读/异步写；50-100KB 限额；按 user_id 隔离；多标签 last-write-wins（带时间戳） | 刷新、换设备、换浏览器状态一致 |
| ✅ 2.4 使用记录抽屉 UI 升级 | 时间线展示（打开/操作/自动保存），每条可"恢复到当时状态"，支持搜索；空态引导 | 抽屉不再是简单的动作列表 |
| ✅ 2.5 游客本地墓碑 | 未登录时状态写 localStorage（wewoo-db-*），登录后首次使用自动合并上云 | 游客状态本机可恢复，登录后不丢失 |
| ✅ 2.6 全屏使用记录 | 全屏模式计入同一条状态流，退出全屏不再清状态 | 用户实测纪念日记录器：全屏退出后数据保留 |

### v1.6.1 全屏/交互修复 + 真实工具记忆 ✅ 已完成（2026-08-07）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 2.7 全屏同源 iframe | 全屏改为复用同一个 iframe（CSS fixed 覆盖视口），删除独立的第二个 iframe；修复全屏容器 `fixed` 与 `relative` 样式冲突导致高度崩缩为 0 | 预览玩到一半点全屏 / 退出全屏，游戏进度与输入状态完整保留 |
| ✅ 2.8 首次交互可用 | iframe 挂载即 `opacity:1` 可交互（此前 `opacity:0` 在 iOS Safari 命中测试不可靠，需点一下才能玩）；骨架屏仅在 iframe 未挂载时显示 | 打开工具无需先点击，直接滑动/输入即可用 |
| ✅ 2.9 真实工具接入记忆 | 2048 小游戏、情侣纪念日记录器注入 localStorage 持久化（走坟碑 + 云端同步链路） | 2048 刷新后棋盘/分数还原可续玩；纪念日刷新后主页与设置完整恢复 |

### v1.6.2 教程/提示词适配记忆功能 ✅ 已完成（2026-08-07）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 2.10 默认提示词适配记忆 | AI_PROMPT_TEMPLATE 允许/推荐 localStorage 保存用户数据；移除「不使用 localStorage」过时约束 | 复制提示词给 AI 生成的工具自带记忆 |
| ✅ 2.11 示例模板演示记忆 | 「试试示例」改为记忆计数器（localStorage 保存计数，刷新恢复） | 点 +1 后刷新页面数字仍在 |
| ✅ 2.12 教程/引导同步 | /guide 新增带记忆示例提示词与 FAQ；新手弹窗第 4 步补充记忆说明 | 新手知道工具会自动记住数据 |
| ✅ 2.13 扫描分级优化 | localStorage/sessionStorage 降级为 info（记忆提示），不再误报安全警告 | 发布 localStorage 工具显示蓝色记忆说明而非黄色警告 |

### v1.6.3 默认示例升级 + 通用创作要求 ✅ 已完成（2026-08-07）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 2.14 示例全面启用记忆 | 6 个示例提示词 + 「试试示例」模板（喝水打卡）全部内置 localStorage 记忆要求 | 示例工具刷新/全屏/重进后状态恢复 |
| ✅ 2.15 微坞通用创作要求 | 浓缩版要求卡片（引导页可复制）；默认提示词升级为 7 条结构化要求 | 用户照抄即可生成功能完整工具 |
| ✅ 2.16 创作页示例选择器 | 创作页 AI 提示助手新增 6 个示例提示词一键复制 | 手机端点一下即可复制完整提示词 |
| ✅ 2.17 创作页预览记忆 | 预览 iframe 接入 localStorage 快照 seed（游客本地墓碑，登录按账号隔离） | 创作页预览/全屏/刷新不再丢状态 |

### v1.6.4 内置工具全面升级 ✅ 已完成（2026-08-07）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 2.18 内置工具记忆+完善 | 18 个早期内置工具统一补齐移动端适配 + localStorage 记忆；分账/喝水/冰箱/比价/测验/换算等场景功能增强 | 手机打开全部工具正常；刷新/重进状态保留 |
| ✅ 2.19 大工具增强 | 科学计算器加计算历史；密码生成器历史持久化；螺纹查询加移动端布局与型号记忆；AI 转换器输入自动保存 | 大工具在手机可用且数据不丢 |

### v1.6.5 内置工具记忆/最近使用/使用记录修复 ✅ 已完成（2026-08-08）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 2.20 内置工具云端持久化打通 | 根因：tool_state / tool_usage_history / tool_recent 的 tool_id 为 UUID 类型，内置工具字符串 id（1-18）写入报 invalid input syntax for type uuid，导致登录用户状态保存/恢复、使用记录、最近使用全部失效；新增 src/lib/builtinIds.ts 稳定 UUID 映射，state/history API 统一换算 | 登录态退出后重进恢复上次状态；使用记录可写可查；首页「最近使用」出现内置工具 |
| ✅ 2.21 本地墓碑 key 读写一致 | 工具详情页登录用户 localStorage 快照按账号隔离（wewoo-ls-<id>-<userId>），读取时补齐账号后缀 | 离线/弱网时本地缓存也能恢复 |

### v1.6.6 全屏滚动穿透修复 ✅ 已完成（2026-08-08）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 2.22 全屏滚动不影响预览 | 全屏时锁定页面滚动（overflow hidden）；进入全屏前记录工具内滚动位置、退出后恢复（沙盒新增 WEWOO_GET_SCROLL / WEWOO_SET_SCROLL 消息） | 全屏滑动后退出，预览界面与进入前完全一致，页面不再停留别处 |

### v1.7.0 体验与性能 ✅ 已完成（2026-08-08）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 3.1 首页缓存优先 | localStorage 缓存（wewoo-tools-cache）带 TTL（60s）与结构版本号，首页先同步渲染再后台刷新替换 | 弱网/慢 Supabase 时首屏秒开 |
| ✅ 3.2 请求并行化 | 首页工具列表 + 浏览量并行请求互不阻塞；列表刷新后自动补充新工具浏览量 | 请求瀑布变短，首屏更快 |
| ✅ 3.3 搜索增强 | 服务端搜索 API（/api/tools/search，ilike title/description/author）+ 前端防抖 300ms + 本地内置工具合并去重 | 多工具时搜索仍可用 |
| ✅ 3.4 发布封面自定义 | 发布弹窗新增封面选择（自动截图/上传图片/渐变+表情，默认截图可跳过）；作者详情页可「换封面」 | 作者可自定义封面并随时修改 |
| ✅ 3.6 体验细节 | 移除 30s 自动展开；浏览量会话内去重；emoji 映射收敛到 lib/constants；data.ts 拆分（mock-tools.ts） | 无打扰弹层；数据文件可控 |
| ⏭️ 3.5 适老化第一版 | 用户决定暂缓（不做长辈模式开关），易用性通过全站触控尺寸（≥44px）与流程简化实现 | 后续如需再做 |

> 说明：3.5 适老化经讨论后暂缓，目标通过整体易用性实现（按钮 ≥44px、流程少、直白），不做单独的"长辈模式"开关。

### v1.8.0 内置 AI 生成 ✅ 已完成（2026-08-08）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.1 服务端 AI 生成 API | 新增 `/api/ai/generate`：接收需求描述 → 调 DeepSeek `deepseek-chat`（stream:true, max_tokens 8192）→ SSE 透传前端；`DEEPSEEK_API_KEY` 服务端环境变量 | 浏览器里输入需求即流式出代码，无需复制到外部 AI |
| ✅ 4.2 系统提示词强制白名单 | `src/lib/aiPrompts.ts` 新增 `AI_SYSTEM_PROMPT`：移动端 375px、按钮≥44px、自包含无网络请求、localStorage 记忆、只输出 ```html 代码块、拒绝违规内容 | 生成结果直接可跑、符合沙盒要求、自带记忆 |
| ✅ 4.3 敏感内容过滤 | 需求与生成结果双重 `containsSensitiveContent` 校验（赌博/色情/诈骗/恶意程序/破解外挂） | 违规需求直接拦截并提示分类 |
| ✅ 4.4 创作页 AI 面板 | 创作页 AI 提示助手顶部新增「直接用 AI 生成」：输入框 + 开始/停止生成 + 流式输出 + 填入编辑器/重新生成/复制代码；原提示词复制与示例按钮原样保留 | 手机/电脑均可生成 → 一键填入编辑器 → 右侧实时预览 |
| ✅ 4.5 引导页教程同步 | 教程第 2 步改为「直接用 AI 生成」，第 4 步补充自动填入说明 | 新手知道内置 AI 的用法 |
### v1.8.1 对话式创作 + 多版本切换 ✅ 已完成（2026-08-08）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.6 对话式生成面板 | 创作页 AI 区升级为对话 UI：消息气泡 + 流式回复 + 输入框（Enter 发送）；无历史时自动展开 | 手机/电脑打开创作页直接对话生成，无需先看教程 |
| ✅ 4.7 多版本切换 | 每次生成出代码自动编号 V1/V2…，版本按钮点击即载入完整代码到编辑器并收起对话（回到代码编辑模式）；「＋ 换个版本」生成替代版本 | 多轮对话产生多个版本，来回切换修改对比 |
| ✅ 4.8 对话记忆 | 对话与版本列表按账号存 localStorage，刷新/重进恢复 | 生成到一半刷新，对话和版本还在 |
| ✅ 4.9 改编对话 | 工具详情「改编」加载代码后自动展开对话并提示可继续对话修改；请求携带当前代码上下文 | 改编工具直接说「换个配色」即可出新版 |
| ✅ 4.10 权限安抚反馈 | 系统提示词新增权限边界规则：沙盒外需求先温和说明原因，再给可运行替代方案（localStorage 模拟等） | 实测「要服务器+微信支付」需求：AI 说明原因并输出本地记账本替代 |
| ✅ 4.11 教程配套 | 引导页第 2-4 步改为对话生成→载入完整代码→外部 AI 可选 | 新手按教程走通对话生成全流程 |
### v1.8.2 生成自动填入 + 提取修复 ✅ 已完成（2026-08-08）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.12 载入混入文字修复 | extractHtmlFromAiOutput 重写：优先 ```html/``` 代码块，无代码块时截取 <!DOCTYPE/<html> 到 </html> 文档段，无闭合标签时需含 <body>/<head> 才接受 | 生成结果自动填入编辑器后无 AI 说明文字混入 |
| ✅ 4.13 生成自动填入 | 每次生成完成自动 setCode 填入编辑器，不再需要点版本按钮；版本按钮保留用于切换历史版本 | 生成结束即可点「预览」看效果 |
| ✅ 4.14 版本摘要 | 版本按钮显示需求摘要（V1·喝水打卡 / V2·改成红色主题） | 多个版本一眼区分 |
| ✅ 4.15 快捷场景 | 对话空态显示 6 个快捷场景按钮，点一下直接生成 | 零输入即可开始 |
### v1.8.3 安全与成本紧急修复 ✅ 已完成（2026-08-08）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.16 泄漏密钥移除 | AI_HANDOFF.md 移除 DeepSeek API Key 与 Supabase Service Role Key；两个路由删除硬编码 service_role key，改为环境变量读取 | 仓库 grep 无密钥；缺环境变量时明确 500 |
| ✅ 4.17 AI 上下文压缩 | 多轮对话只保留最近 8 条用户需求 + 当前代码，不再重发历史完整 HTML；currentCode 上限 24000 字符 | 单次请求输入 token 大幅下降 |
| ✅ 4.18 限流 + 余额保护 | 每 IP 8 次/分钟、120 次/天；DeepSeek 余额低于 ¥1 拒绝生成 | 超限 429；低余额拒绝 |
| ✅ 4.19 AI 用量日志 | 流式返回捕获 usage，输出 [ai-usage] 日志 | Vercel Logs 可查每次 token 消耗 |### v1.8.4 模型成本优化 ✅ 已完成（2026-08-08）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.20 模型钉死 flash | route 显式使用 deepseek-v4-flash（环境变量 AI_MODEL 可覆盖），杜绝误用 pro | 用量记录全部为 flash |
| ✅ 4.21 用量日志带模型 | [ai-usage] 日志新增 model 字段 | Vercel Logs 可核对每次生成的模型 |
### v1.8.5 生成空白紧急修复 ✅ 已完成（2026-08-08）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.22 关闭思考模式 | route 对 v4 系模型显式 `thinking:{type:"disabled"}`，正文不再被 reasoning 挤占 | 生成完整 HTML、content 非空，单次 token 约 2300 |
### v1.8.6 创作体验重构 ✅ 已完成（2026-08-08）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.23 三 tab 布局 | 移动端「对话/代码/预览」三 tab；桌面宽屏三栏同时显示（AI Chat Panel 独立左栏） | 手机三视图切换正常；桌面三栏并存 |
| ✅ 4.24 对话隔离 | 改编工具清空旧对话只保留「已加载」消息；对话区新增「新建对话」按钮 | 改编不再混入上个工具对话 |
| ✅ 4.25 外部提示词折叠 | 「想用外部 AI」提示词与示例默认收起、点击展开 | 创作页信息密度降低 |
| ✅ 4.26 安装到主屏幕提示 | 新增 InstallPrompt：桌面浏览器「安装微坞」按钮 + iOS「添加到主屏幕」引导 | PWA 安装入口可见 |
| ✅ 4.27 生成自动切代码 tab | AI 生成完成自动切到「代码」tab | 移动端生成后直接看到代码 |
### v1.8.7 创作体验优化 ✅ 已完成（2026-08-08）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.28 预览直通全屏 | 移动端「预览」tab 点击直接全屏，退出回代码 tab | 手机上点预览即全屏 |
| ✅ 4.29 输出上限放松 | max_tokens 默认 16000（AI_MAX_TOKENS 可覆盖） | 3 万字代码完整生成（finish_reason=stop） |
| ✅ 4.30 输入上下文放松 | currentCode 上限 32000、总字符护栏 48000 | 改编长工具代码不再被裁剪 |
### v1.8.8 工具添加到主屏幕 + 登录保持 ✅ 已完成（2026-08-08）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.31 单工具 PWA manifest | `/tool/[id]/manifest.webmanifest` 动态生成，封面作图标，start_url 带 `?app=1` | 工具页 manifest 链接有效、JSON 正确 |
| ✅ 4.32 添加到主屏幕按钮 | 工具详情页新增安装按钮：Android/桌面 beforeinstallprompt + iOS「分享→添加到主屏幕」引导 | 手机/桌面均可见安装入口，已安装自动隐藏 |
| ✅ 4.33 主屏幕打开自动全屏 | `?app=1` 或 standalone 自动进入全屏，右上角变「🏠 返回广场」 | 主屏幕打开工具即全屏可用 |
| ✅ 4.34 登录跳回 | 工具页登录入口带 redirect，成功后回原工具；`/auth/callback` 透传 redirect | 未登录点收藏→登录→回到原工具 |
### v1.9.0 PWA 全站入口修复 + UI 升级第一批 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.35 PWA 全站入口修复 | appMode 仅由 `?app=1` 触发；全站主屏幕入口（standalone 无参数）点工具正常进详情页 | standalone 下详情页不自动全屏；?app=1 自动全屏 |
| ✅ 4.36 登录状态提示 | PWA 入口未登录时首页顶部提示条 + 去登录按钮；Navbar 移动端登录按钮品牌渐变 | standalone 未登录可见提示条 |
| ✅ 4.37 设计 token 化 | 品牌渐变/圆角/阴影 CSS 变量（brand-gradient 等） | 全局样式引用可用 |
| ✅ 4.38 移动端触控规范 | 详情页操作栏、创作页 tab/操作按钮统一 ≥44px；字号 10px→12px+ | Puppeteer 实测无 <44px 按钮 |
| ✅ 4.39 移动端字号规范 | 正文 15px、h1 22px、h2 18px；输入框 16px 防 iOS 缩放 | 手机可读性提升 |
### v1.9.1 单工具主屏幕体验完善 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.40 standalone 单工具安装引导 | 全站 PWA 已安装（standalone）时工具页安装按钮仍显示，弹窗引导复制链接 → Safari/Chrome 打开 → 添加到主屏幕 | standalone 下可把单工具加到主屏 |
| ✅ 4.41 全屏去掉返回广场 | 单工具 PWA（?app=1）全屏不再显示「返回广场」，靠系统手势返回 | 全屏更纯粹 |
| ✅ 4.42 全屏登录提示条 | 单工具全屏未登录时顶部提示「登录后进度和记录可云端保存」+ 登录按钮（带 ?app=1 跳回全屏） | 未登录全屏可见提示，登录后回全屏工具 |
### v1.9.2 广场卡片 UI 优化 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.43 封面纯净化 | 移除封面类别/浏览量/下载型黑底徽章，封面只留图片或渐变+大 emoji | DOM 检查封面无文字徽章 |
| ✅ 4.44 卡片信息分层 | 标题 15px，作者+浏览量同排浅灰小字，下载型改正文小标 | 卡片信息集中在 body |
### v1.9.3 工具详情页 UI 优化 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.45 元信息行精简 | 作者/日期/浏览量浅灰小字，移除蓝色高亮与行内「使用记录」 | 头部信息清爽 |
| ✅ 4.46 操作栏主次分层 | 主操作 44px（点赞/收藏/分享/改编/全屏/安装）+ 次要操作浅色小字（使用记录/换封面/删除） | 操作区不再一排混排 |
| ✅ 4.47 按钮文字补齐 | 分享/收藏按钮带文字标签 | 语义清晰 |
### v1.9.4 信息密度压缩 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.48 详情页头部压缩 | 标题与徽章同行、描述 line-clamp-2、改编自并入 meta 行 | 头部 3 行内 |
| ✅ 4.49 首页压缩 | 删 Hero 副标题、卡片描述 1 行、区块标题收紧 | 首屏信息密度下降 |
### v1.9.5 首页布局优化 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.50 标题上移 | 「发现实用小工具」移入导航栏 logo 旁（桌面+手机） | 首屏不再有独立大标题 |
| ✅ 4.51 分类/排序下移 | 分类与最新/热门移到最近使用/我的工具下方，与广场网格在一起 | 首页结构：搜索→最近使用→我的工具→广场 |
### v1.9.6 创作体验统一 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.52 首页标题文案 | 「发现实用小工具」改为「AI 工具广场」 | 导航栏标题与广场区块呼应 |
| ✅ 4.53 创作页对话浅色化 | AI 对话面板/头部/输入框/气泡/快捷按钮全部浅色，保留品牌靛蓝 | 创作页与全站浅色 UI 一致，行为零改动 |
| ✅ 4.54 代码编辑器墨黑统一 | 编辑器背景改为品牌墨黑 #0A1628，头部/快照栏/边框配套 | 深色编码习惯保留，观感与设计系统一致 |
### v1.9.7 首页视觉优化 + 品牌化 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.55 大标题回 Hero + 搜索压缩 | 标题独立一行（text-xl/2xl），搜索栏变图标与标题同排，点击展开 | 首屏标题醒目，搜索不占行 |
| ✅ 4.56 默认封面品牌化 | 兜底封面/无封面占位统一品牌渐变 #5046E5→#8B5CF6 + W 元素 | 新工具封面带品牌识别 |
| ✅ 4.57 设计 token 补全 | 字体栈 Noto Sans SC/Inter、背景灰 #F9F9FB、分割线 #E7E7EA、卡片阴影规范 | 全局观感与设计系统一致 |
### v1.9.8 稳定修复 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.58 修复工具误报语法错误 | 就绪计时 effect 依赖改为只随工具/预览地址，savedState/user 改走 ref | 手机端 2048 等工具长时间游玩不跳「语法错误」 |
| ✅ 4.59 修复单位换算大全报错 | 补回缺失的 save() 函数 | 点击重量等分类正常切换 |
### v1.9.9 稳定修复 + 创作页视觉统一 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.60 修复 iOS 添加主屏幕变 we-woo.net | 每页只注入一个 manifest：站点 manifest 改手动路由 + SiteManifestLink 按页面注入 | 工具页添加到主屏幕稳定打开该工具 |
| ✅ 4.61 作者行对齐 | 作者/改编自链接文字在 44px 触控区内垂直居中 | 名字与日期同一行对齐 |
| ✅ 4.62 相关工具封面 | 有封面显示封面图，无封面渐变+emoji 回退 | 推荐卡片与首页卡片观感一致 |
| ✅ 4.63 创作页字号统一 | 气泡 text-sm、次要信息 text-xs，去掉 10/11px 零散字号 | AI 对话区层级清爽统一 |
### v1.9.10 体验小债修复 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.64 注册昵称 | 注册表单加可选昵称，存 user_metadata.name；主页/发布/评论统一展示 | 他人主页显示真实昵称而非 UUID 前缀 |
| ✅ 4.65 主页头像/标题兜底 | 无工具用户回退「微坞用户」+ 品牌头像，不再显示 "?" | 任意用户主页头像标题正常 |
| ✅ 4.66 分类 emoji 统一 | 工具页本地 emoji 映射改引 lib/constants | 无重复定义，改分类 emoji 只改一处 |
### v1.9.11 稳定修复：手机端 12 秒误报「语法错误」 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.67 修复 12s 误报 | 错误计时器仅在 iframe 真正挂载后启动（stateLoaded && docReady 加入依赖）；误报页新增「重新加载」按钮 | 慢网络/登录用户打开工具不再 12s 误杀，误报可一键重试 |
| ✅ 4.68 状态加载兜底 | 状态接口 10s 兜底放行 iframe 挂载；云记忆在接口返回后补注入 | 状态请求再慢工具也能打开，记忆不丢失 |
### v1.11.0 教程入口补全 + 创作页 UI 整理 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.69 创作页教程按钮 | 移动端顶栏书图标按钮 + 桌面端操作栏「教程」文字按钮，均链到 /guide 新标签打开 | 登录后任意 tab 都能找到教程入口 |
| ✅ 4.70 对话空状态引导 | AI 对话为空时展示「📖 不会写？看看教程，5 分钟上手」链接 | 新用户打开创作页第一屏就能看到教程 |
| ✅ 4.71 登录后教程横幅 | 首页新手教程横幅游客/登录分开记忆（独立 localStorage key），登录用户可再看一次；文案按登录状态适配 | 登录账户不再「找不到教程」 |
### v1.11.1 稳定修复：手机端 12 秒误报根治 + 创作页按钮统一 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.72 修复 12s 误报（根治） | READY 确认从 effect 局部变量改为按 iframe 实例（tool+srcDoc+重试计数）记忆；监听器挂载后主动 PING iframe 补收 READY（sandbox STORAGE_API 回复）；iframe onLoad 作为兜底确认；「重新加载」清空确认并重建 iframe 重新计时 | 2048/情侣纪念日手机端长时间游玩与静止均不误报，记忆/全屏正常 |
| ✅ 4.73 创作页顶部按钮统一 | 移动端「教程」改图标+文字，与「示例」统一白底靛蓝描边次级按钮样式，主操作（保存/发布）保持实色/渐变 | 顶栏按钮层级清晰、样式统一 |
### v1.12.0 P2 设计系统深化（第一批）：token 落地 + 组件化 ✅ 已完成（2026-08-09）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.74 token 完整落地 | globals.css 补圆角 12/20/35、文字层级、背景层级、页面宽度、brand-200；青色 #22D3EE 统一走 --accent-cyan；语义工具类 panel/btn-secondary/btn-ghost/btn-danger/input-base/radius-*/text-secondary/text-muted/bg-soft/page-width | 关键页面计算样式：panel=白底 12px 圆角 line 边框，btn-secondary=靛蓝描边，Badge 徽章统一 |
| ✅ 4.75 弹窗组件化 | src/components/ui.tsx 新增 Modal（遮罩/卡片/进出场动画统一），6 处弹窗接入：新手引导/安装引导/发布工具/发布成功/更换封面/删除确认 | 各弹窗打开/关闭行为与旧版一致（Puppeteer 实测） |
| ✅ 4.76 徽章组件化 | Badge 组件落地并替换：工具详情分类/刚刚发布/可见性、首页下载型徽章 | 徽章样式统一为浅底深字圆角小标 |



### v1.12.1 暗色模式（P2 第二批）✅ 已完成（2026-08-10）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.77 主题切换机制 | 新增 ThemeToggle（桌面端操作栏 / 移动端头像区）+ 首帧内联脚本：localStorage「wewoo-theme」优先，否则跟随 prefers-color-scheme；切换即时生效并同步 theme-color meta | 深/浅切换、刷新记忆、跟随系统均 Puppeteer 实测通过 |
| ✅ 4.78 暗色样式全量适配 | :root[data-theme="dark"] 变量覆盖 + Tailwind 硬编码类覆盖（bg-white/gray-*/text/border/状态色及 hover/focus/group-hover 变体）；页面根统一 bg-page；bg-white/95 毛玻璃、引导页渐变、输入框边框暗色适配；修复注释内嵌 `*/` 导致 dark 变量规则被浏览器丢弃的编译 bug | 9 类页面（首页/创作/登录/引导/工具/变更日志/隐私/条款/清理）暗色零浅色残留；浅色模式像素级零改动 |

### v1.12.2 UI 形状统一与圆角修复 ✅ 已完成（2026-08-10）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.79 圆角体系修复 | 业务圆角 token 改名（--radius-control/--radius-hero），不再占用 Tailwind --radius-* 命名空间；全站 rounded-lg 从误改的 35px 恢复标准 8px | create 页顶部 4 按钮、首页最新/热门等 46 处按钮圆角统一（Puppeteer 实测） |
| ✅ 4.80 UI 形状统一 | create 顶部按钮字号对齐；首页排序改胶囊；桌面端登录 44px/12px；封面 emoji 圆形背景；教程横幅图标格圆形；用户页卡片 16px | 浅色模式零变化、暗色模式无回归；tsc 通过 |


### v1.13.1 个人头像与分享优化 ✅ 已完成（2026-08-11）

| 编号 | 事项 | 实现 | 验证 |
| --- | --- | --- | --- |
| ✅ 4.88 个人头像更换 | 用户主页更换按钮 → 图片压缩 512×512 WebP → 上传 tool-covers/avatars/ 路径 → 写入 auth.users user_metadata.avatar_url；导航栏（桌面/移动）与用户主页统一 Avatar 组件（有图显示、无图首字母渐变） | 全站头像即时更新（AuthProvider USER_UPDATED） | 实测上传：storage 上传成功 + metadata 更新 + 页面 3 处头像显示；tsc 通过 |
| ✅ 4.89 分享优化 | 工具详情页 OG 分享图改用工具封面（内置工具 WebP 封面、用户工具 Supabase 封面、无封面兜底 og.png），描述带作者；用户主页新增 OG metadata（昵称 + 发布数量） | 微信/社交分享卡片显示工具封面 | og:image/title/description 实测正确（用户工具 + 内置工具） |

### v1.13.0 提速与收尾 ✅ 已完成（2026-08-10）

| 编号 | 事项 | 实现 | 验证 |
| --- | --- | --- | --- |
| ✅ 4.85 内置封面转 WebP | 18 个内置工具封面 PNG → WebP（质量 82，method 6），代码引用全部切到 `/covers/*.webp`，旧 PNG 保留兼容缓存 | 首页封面网络体积 -72%（1726KB → 478KB） | tsc 通过；Puppeteer 首页 32 封面全加载 |
| ✅ 4.86 详情数据缓存秒开 | fetchToolById 成功后写 localStorage 详情缓存（24h TTL、最多 10 条），详情页进入时同步预填立即渲染 iframe，网络结果覆盖保持最新 | 再次打开工具 iframe 秒开，首次打开不变 | tsc 通过；缓存命中/刷新逻辑代码审查 |
| ✅ 4.87 ROADMAP 状态整理 | 勾选已修复技术债 B1/B2/B4/B5/B8，P2 暗色模式/组件化、P3 字段瘦身标记完成 | 文档与代码实际状态一致 | 逐项对照代码确认 |

### v1.12.4 最近使用封面与手机端修复 ✅ 已完成（2026-08-10）

| 编号 | 事项 | 实现 | 验证 |
| --- | --- | --- | --- |
| ✅ 4.82 最近使用封面修复 | recent-tools 接口补 coverUrl（db 工具 cover_url / 内置工具公共封面），首页最近使用卡片 `String(undefined)` 误判修复为安全判断 | 电脑端最近使用封面正常，无 src=undefined | API 实测返回 coverUrl；Puppeteer 手机视口：最近使用 2 卡封面加载成功无破图，首页 32 封面全加载 |
| ✅ 4.83 无封面破图兜底修复 | mapRow / 搜索接口 / fetchTools 合并不再把无封面工具兜底成不存在的 `/covers/<uuid>.png`，统一渐变占位，仅内置工具保留公共封面 | 用户发布工具无封面不再破图 | Puppeteer 滚动加载 32/32 成功；tsc 通过 |
| ✅ 4.84 手机端最近使用不消失 | 401/网络失败保留本地缓存不清空；authedFetch 遇 401 自动 refreshSession 重试一次 | 手机 token 过期不再导致最近使用区块消失 | 登录态手机视口实测最近使用正常显示 |
### v1.12.3 工具卡片封面统一 ✅ 已完成（2026-08-10）
| 任务 | 改动点 | 验收 |
|------|--------|------|
| ✅ 4.81 卡片图标改用封面 | 个人主页（收藏/发布）、首页（广场/最近使用/我的工具）、工具详情页相关推荐：有 coverUrl 显示封面图（next/image fill + object-cover），无封面显示纯渐变占位，移除 getToolEmoji 图标 | 用户页 11 卡片全封面、首页 32 卡片全封面，无 emoji（Puppeteer 实测）；tsc 通过 |

### v1.15.2 iOS 主屏快捷方式地址修复 + 封面上传策略修复（2026-08-12）

> ⚠️ 部署后需在 Supabase Dashboard → SQL Editor 执行一次 `supabase_fix_rls_storage_v2.sql`（修正 Storage 上传策略，恢复封面/头像上传）

| 编号 | 任务 | 改动点 | 验收 |
|------|------|--------|------|
| ✅ 4.99 | iOS「添加到主屏幕」被存成官网首页 | 工具页 manifest URL 版本化（/tool/[id]/manifest.webmanifest?v=2）+ manifest 响应 Cache-Control: no-store；根因：iOS 顽固缓存 manifest，且早期工具页曾同时注入站点 manifest（start_url "/"） | 工具页仅一个 manifest；?v=2 返回正确 start_url；用户删旧快捷方式 + 清网站数据后重新添加，弹窗显示工具 URL |
| ✅ 4.100 | 封面上传 403（发布后无封面） | supabase_fix_rls_all.sql 的 Storage 策略用 foldername()[2] 恒 NULL → 改用 storage.filename() + split_part 解析；新增 supabase_fix_rls_storage_v2.sql 供线上快速修复 | 执行 v2 SQL 后，作者可上传/更换封面与头像；新发布工具有封面 |

### v1.15.4 封面链路彻底防挂起（2026-08-12）

| 编号 | 任务 | 改动点 | 验收 |
|------|------|--------|------|
| ✅ 4.102 | v1.15.3 后 iPhone Safari 封面生成仍卡死 | 根因：captureCover 最后一步 canvas.toBlob 在 iOS 上可能永不回调且无超时保护 → 新增 canvasToBlobWithTimeout（8s），toBlob 超时返回 null 走渐变兜底；upload 模式 dataURL→Blob 加 8s 超时（dataUrlToBlob）；uploadCoverToStorage 整体加 25s 超时（withTimeout 包装，避免网络挂起无限等待） | iPhone Safari 发布/更换封面 30s 内必完成（正常截图/上传或渐变兜底），不再永久卡在「正在生成封面」；桌面 Chrome E2E 自动截图 11s、上传图片 4.6s 均正常，写库与清理通过 |
| ✅ 4.103 | 快照 iframe srcdoc 兼容 | srcdoc 以 <!DOCTYPE 开头在部分浏览器（尤其 iOS Safari）可能空白/加载异常 → sanitizeSnapshotHtml 先剥离前缀 doctype 再渲染 | 桌面/安卓截图结果与之前一致，无回归 |

### v1.15.5 封面截图服务端化 + 上传读取失败修复（2026-08-13）

| 编号 | 任务 | 改动点 | 验收 |
|------|------|--------|------|
| ✅ 4.105 | 上传封面「图片读取失败」（手机+电脑） + 换封面后首页不更新 | dataUrlToBlob 从 fetch(dataURL) 改为 atob 同步解码（根因：CSP connect-src 不含 data:，fetch(data:) 被浏览器拦截，v1.15.4 引入）；封面 URL 加版本参数（?v=时间戳）绕过浏览器/next-image 旧图缓存 + invalidateToolCaches 失效首页/详情/个人页本地缓存 | 手机/电脑上传图片封面立即成功且保存后首页立即显示新封面；tsc/eslint 通过 |
| ✅ 4.106 | iPhone Safari 自动截图仍失败 | 新增服务端截图 POST /api/cover/screenshot：Puppeteer 无头浏览器渲染（Vercel 用 @sparticuz/chromium，本地用 puppeteer），375x667 PNG；客户端 captureCover 服务端优先，失败回退 html2canvas → 渐变封面；共享封面 HTML 包装（coverHtml.ts，CSP 与沙盒一致） | iPhone Safari 发布/更换封面出真实截图，不再走渐变兜底；桌面无回归；本地 Puppeteer 实测 60KB PNG（含 canvas + alert 自动关闭） |
| ✅ 4.108 | 自动截图生产 500 | @sparticuz/chromium 加入 serverExternalPackages（防 bundle 破坏 import.meta.url 相对路径），outputFileTracingIncludes 显式包含 node_modules/@sparticuz/chromium/bin/** 与 fonts/**；服务端 maxDuration 90s，客户端超时 75s；错误响应附 detail；无头环境文字空白根因：FONTCONFIG_PATH 必须指向目录（fontconfig 在其中读 fonts.conf），且 fonts.tar.br 解压会覆盖配置，需在 executablePath() 后重写 fonts.conf | 生产 POST /api/cover/screenshot 返回 200 PNG（含中文+emoji）；发布/更换封面 iPhone 出真实截图 |
| ✅ 4.109 | 渐变封面无 emoji（iOS） | 新增服务端 POST /api/cover/gradient：无头浏览器原生渲染渐变 + emoji + 标题 + 品牌角标；generateCustomCoverBlob 服务端优先、本地 canvas 兜底；Vercel 函数注入 NotoSansSC/NotoColorEmoji 字体 + FONTCONFIG_PATH | iPhone 渐变封面含 emoji 与中文标题，与电脑一致；上传图片/自动截图路径无回归 |
| ✅ 4.107 | iOS 渐变封面兜底失效 | canvasToBlobReliable：canvas.toBlob 8s 超时后回退 canvas.toDataURL + atob，渐变封面在 iOS 上也能生成上传 | iOS 上渐变封面可正常保存；桌面行为不变 |

### 运维：E2E 测试数据清理（2026-08-12）

| 编号 | 任务 | 状态 | 说明 |
|------|------|------|------|
| ⏳ 4.104 | E2E 测试数据防残留 | 发现线上残留测试工具「E2E自动截图-678692」（bff0fb46，测试账号），按 v1.15.5 用户反馈：线上仍可见测试工具「E2E自动截图-678692」，需在 Dashboard 删除工具记录 + tool-covers 封面文件后复检 | 后续 E2E 脚本统一改为 try/finally 清理，跑完自动校验「title 含 E2E 的工具 = 0」；发布/封面 E2E 完成后若异常中断需人工复查 |

### v1.15.3 自动截图卡死修复（2026-08-12）

| 编号 | 任务 | 改动点 | 验收 |
|------|------|--------|------|
| ✅ 4.101 | 发布/更换封面一直卡在「正在生成封面」 | captureCover 第二阶段 html2canvas 无超时保护，iOS Safari 下 promise 可能永不返回 → 加 15s 超时（Promise.race + catch 防 unhandled rejection），超时返回 null 走默认渐变封面；详情页更换封面截图失败自动落默认渐变封面并 toast 提示 | iPhone Safari 发布新工具/更换封面不再卡死：15s 内自动完成（正常截图或渐变兜底），原有上传图片/渐变模式不受影响 |

### v1.15.0 安全加固（P0/P1，2026-08-12 双 AI 审查驱动）

> ⚠️ 部署后需在 Supabase Dashboard → SQL Editor 执行一次 `supabase_fix_rls_all.sql`（全表 RLS + Storage owner 约束 + `ai_rate_limit` 表/`ai_rate_bump` RPC；幂等可重复执行）。执行前 4.90/4.95 的线上防护不生效（代码已就绪，限流未部署时自动回退内存计数）。

| 编号 | 任务 | 改动点 | 验收 |
|------|------|--------|------|
| ✅ 4.90 | RLS 全表启用（P0） | 在 tools/favorites/reviews 已有策略基础上，补齐 likes / user_pinned_tools / tool_state / tool_usage_history / tool_drafts / tool_recent 的 RLS + owner 读写 + 公共读策略；⚠️ 前端 src/lib/data.ts 用 anon key + 登录 session 直读这些表，策略必须兼容现有查询（owner 可读 + 公共读），逐个表用匿名 key 复测并回归（点赞/收藏/评论/最近使用/记忆/草稿） | 匿名 key 无法直读任何用户行为表；现有功能逐表回归通过 |
| ✅ 4.91 | captureCover 沙箱修复（P0） | 去掉封面截图 iframe 的 allow-same-origin（src/lib/cover.ts:35-37），先验证 blob URL 或服务端截图（puppeteer 已在依赖）可行性再落地 | 用户 HTML 无法读写父页 localStorage/token；封面截图功能不坏 |
| ✅ 4.92 | lint 修复（P1） | package.json lint 改 eslint .（Next 16 已移除 next lint），删除遗留 .eslintrc.json，接入 eslint.config.mjs | npm run lint 本地可跑通过 |
| ✅ 4.93 | 可见性落库（P1） | 发布/编辑表单与 handleChangeVisibility 切换都写入 Supabase visibility 字段 | 切换公开/未列出/私密后刷新保持 |
| ✅ 4.94 | 搜索参数化（P1） | /api/tools/search 不再把 q 拼进 .or() 字符串，改参数化 ilike / 转义特殊字符 | 注入 payload 无效果，正常搜索不回归 |
| ✅ 4.95 | Storage owner 校验（P1） | cover_auth_update 策略加 owner 路径约束（storage.foldername(name)[1] = auth.uid()::text） | 登录用户无法覆盖/删除他人封面 |
| ✅ 4.96 | 限流加固（P1） | AI 生成接口限流从模块级 Map 升级为可跨实例方案（Upstash/DB 计数或 Vercel WAF 优先），x-forwarded-for 取可信来源 | 多实例/冷启动下仍生效；正常使用不受影响 |

> P2 候选（可并入后续 patch，不阻塞发布）：浏览量落库（内置工具）、发布幂等（防双击重复 insert）、anon key 收敛（3 处硬编码 fallback）、robots 与 sitemap 矛盾修正（robots 禁 /create /auth 但 sitemap 收录）、GitHub Actions 基础 CI（lint/typecheck/build）。

### v2.0 内核升级：权限透明反馈（major，涉及沙盒/注入体系）
### v2.0 内核升级：权限透明反馈（major，涉及沙盒/注入体系）

> **2.0 里程碑拆分（2026-08-13）：** 1.x 收尾项全部并入 2.0，不再单独发 1.x 版本。
> - **M1 · v2.0.0 基础设施 + 权限透明反馈**（容量地基 → 能力清单/徽章 → 越权反馈 → 审核底线 → 注册表单简化）✅ v2.0.0 已发布（2026-08-13），M1.5 审核底线 ✅ v2.0.1 已发布（2026-08-14）：is_banned 公开过滤 + 管理后台下架/恢复 + 举报处理
> - **M2 · v2.0.1 零风险 API 第一批**（copyText / download / share / user + Worker/语音验证 + 授权中心）
> - **M3 · v2.1.0 AI 网关 + 联网代理**（wewoo.ai + wewoo.fetch + 配额 + 用量看板，见 v2.0-A/B）
> - **M4 · v2.2.0 社区收尾**（场景模板库 + 热门榜单 + 通知中心 + 个性化首页 + 草稿箱 + 评论 @/图片）
> 红线：纯新增注入 / wewoo 守卫 / 特性检测降级 / 不动 CSP·sandbox·现有消息 / 内置工具全量回归。


| 任务 | 改动点 | 验收 |
|------|--------|------|
| 工具能力徽章 | 工具详情页/预览页展示该工具的能力清单：✅ 本地记忆 / ✅ 音效 / ❌ 联网 / ❌ 系统通知 / ❌ 外部跳转（由代码扫描结果生成） | 用户打开任何工具都能一眼看到能/不能做什么 |
| 发布能力清单 | 发布时把危险 API 扫描结果升级为「能力清单」展示给作者（哪些调用会被拦截/限制） | 作者发布前明确知道工具边界 |
| 运行时越权反馈 | 工具试图 fetch / 弹窗等越权操作 → 静默拦截 + 页面提示「此工具没有 X 权限」 | 不白屏、不静默无感 |
| AI 能力说明 | AI 生成/改编完成后，对话里自动附一句「本工具可以/不可以做什么」 | 用户生成完就知道边界 |

> **v2.0 沙盒能力第一批（2026-08-13 用户批准）：AI 网关 + 白名单网络代理**。
> 复用现有 AI 基础设施（/api/ai/generate：DeepSeek key 服务端持有、IP 限流、余额保护 MIN_BALANCE_CNY、敏感词检查、上下文压缩、flash 低价档、usage 记账），不重复造轮子。

#### v2.0-A · AI 网关（wewoo.ai）——工具运行时 AI 接入

| 任务 | 改动点 | 验收 |
|------|--------|------|
| 沙盒 API 注入 | 新增 `__wewoo.ai.chat({ prompt, context? })`（非流式）与 `__wewoo.ai.stream({ prompt, onChunk })`（流式），走 postMessage → 服务端代理；AI 系统提示词教「特性检测 + 降级」写法（无 `__wewoo.ai` 时提示"该工具需要联网 AI，请在微坞内打开"） | 工具内一行调用即可问答；沙盒外打开优雅降级提示 |
| 服务端代理接口 | 新增 POST /api/ai/tool（登录 + 工具级鉴权）：组装系统提示词（固定，工具代码与用户输入分层防注入）→ DeepSeek flash → 流式返回 + usage 记账（工具 ID / 用户 ID / 成本） | key 永不下发浏览器；每次调用落用量日志 |
| 权限声明与授权 | 发布时勾选「使用 AI」→ 能力徽章 + 详情页展示；用户首次使用弹授权（可撤销）；未授权/未声明 → 拦截并提示 | 作者发布前明确声明，用户使用前明确知情 |
| 配额与成本控制 | 三级配额：平台免费额度（每用户每天 N 次，初期 20，按成本调）→ 工具作者可为自己的工具配置额度 → 全局熔断（余额 < 阈值拒绝，复用现有逻辑）；模型钉死 deepseek-v4-flash，禁止工具指定模型 | 用户烧钱风险可控；超限提示「作者未开启 AI 或额度已用完」 |
| 用量看板 | [ai-usage] 日志升级为数据库表 ai_usage（工具/用户/模型/token/成本），管理后台/作者页展示；日账单 + 异常突增告警 | 成本透明，异常可追溯 |
| 内容安全 | 输入输出敏感词检查（复用 containsSensitiveContent）；对话内容不留存或脱敏 | AI 对话不产生违规内容 |

#### v2.0-B · 白名单网络代理（wewoo.fetch）——工具联网能力

| 任务 | 改动点 | 验收 |
|------|--------|------|
| 沙盒 API 注入 | 新增 `__wewoo.fetch(url, { method?, headers?, body? })` → postMessage → 服务端代理；仅支持白名单域名；响应大小上限（默认 256KB） | 工具可读公开 API；无网络请求被直接放行 |
| 服务端代理接口 | 新增 POST /api/proxy（登录 + 工具级鉴权）：域名白名单校验（配置化，初期：汇率/天气/词典/翻译/二维码/公开数据类）+ 仅 GET（初期）+ 限流（按工具 + 用户）+ 审计日志 | 白名单外域名一律拒绝；每次调用可审计 |
| 白名单管理 | 域名白名单配置（代码内常量 → 后期管理后台可配），平台审核新增域名 | 作者不可自行添加白名单域 |
| 安全红线 | 永不 allow-same-origin；不开放任意 URL；不代持 Cookie；响应做 sanitize（防 XSS 载荷注入工具 DOM）；超时 10s | 工具联网不引入主站风险 |

#### v2.0-C · 配套（共用）

| 任务 | 改动点 | 验收 |
|------|--------|------|
| 授权中心 | 用户可查看/撤销每个工具的 AI / 联网授权（个人页入口） | 授权可管可控 |
| 兼容性红线 | 遵守现有沙盒兼容 5 条（纯新增注入 / wewoo 守卫 / 特性检测降级 / 不动 CSP·sandbox·现有消息 / 内置工具全量回归） | 已存在工具零行为变化 |

#### v2.0-D · 零风险能力第二批（2026-08-13 用户确认，来自双 AI 审查权限清单）

> 取舍原则：只取「低风险 + 刚需 + 不破坏兼容」的；中高风险（通知/摄像头/麦克风/定位）保持暂缓。

| 任务 | 原表优先级 | 改动点 | 验收 |
|------|-----------|--------|------|
| 剪贴板写入验证 + 兜底代理 | P0 | 验证沙盒内 `navigator.clipboard.writeText` 各端可用性（内置工具已在用，桌面 OK）；iOS Safari 不可用时父页面代理 `__wewoo.copyText(text)` → postMessage → 父页面写入 | 工具内一键复制结果在手机/电脑都可用；**永不开放剪贴板读取** |
| 文件导出（`__wewoo.download`） | P0 | 新增沙盒 API：`__wewoo.download({ filename, content, mime })` → postMessage → 父页面 Blob 下载（复用现有下载机制）；图片/CSV/报告/日历文件导出 | 图片工具可保存图片、办公工具可导出 CSV；沙盒外降级提示 |
| Web Worker 验证 | P0 | 验证沙盒 iframe 内 `new Worker(blob:)` 可用性（父页 CSP `worker-src blob:` 已就绪）；可用则文档化 + 写进 AI 提示词（大数据计算/图片处理用 Worker 防卡 UI） | 物理游戏/大计算工具 UI 不卡；不可用则评估 iframe 内 CSP 放行 |
| 语音朗读验证 + 提示词 | P0 | 验证 `speechSynthesis` 在沙盒内可用；AI 系统提示词内置「朗读」标准写法（`speechSynthesis.speak` + 中文语言参数 + 按钮 44px） | 学习/朗读/游戏音效类工具可直接生成可用代码 |
| 分享卡片（`__wewoo.share`） | P1 | 父页面拼装分享文案/图片，与 v1.16「微信分享卡片/复制文案优化」合并落地：`__wewoo.share({ text })` → 父页面唤起原生分享 / 复制 | 工具结果一键生成分享文案，微信生态可触达 |
| 只读用户信息（`__wewoo.user`） | P1 | 仅暴露昵称/头像（读自 user_metadata），不暴露邮箱/ID/登录态；用于个性化问候、榜单 | 工具可显示「你好，昵称」；隐私零泄露 |

> 明确**不取**（保留暂缓）：通知 `wewoo.notify`（中风险 + Web Push 复杂度，等真实需求）；摄像头/麦克风/定位（中高风险 + iOS Safari iframe 支持差）；指针锁定（仅游戏类，需求未现）；外部模式 L3（已有长期规划，高风险，上线策略见第十节）。

### v1.9 全站视觉大升级（UI，见第十一章 11.2）
> 纯界面层改动，不涉及沙盒/注入内核，按规范放 minor（1.x.0）。

### 沙盒能力开放（wewoo.*）——第一批已批准（AI + 联网代理，排 v2.0），其余暂缓
- ✅ 已排期（v2.0）：`wewoo.ai`（AI 网关，见 v2.0-A）、`wewoo.fetch`（白名单网络代理，见 v2.0-B）、`wewoo.copyText`（剪贴板写入兜底）、`wewoo.download`（文件导出）、`wewoo.share`（分享卡片）、`wewoo.user`（只读昵称/头像）、Web Worker 验证、语音朗读验证（见 v2.0-D）
- ⏸ 仍暂缓（用户持保留态度）：`wewoo.notify` / 摄像头 / 麦克风 / 定位 / 指针锁定 / 外部模式（L3）
- 触发条件：出现真实需求（工具确实需要通知/系统能力）、产品形态更清晰、鉴权与审核体系就绪
- 若未来开放，必须遵守兼容性红线：
  1. 纯新增注入（shim 之后、用户代码之前），整体 try-catch 包裹，任何异常不影响用户代码
  2. `if (!window.wewoo)` 守卫，不覆盖工具自己定义的变量
  3. 系统提示词教「特性检测 + 降级」写法（`window.wewoo?.xxx`，无则回退 localStorage/默认值），生成的代码在沙盒外（下载型/外部模式）也能跑
  4. 不动 CSP / sandbox / 现有 `WEWOO_*` 消息；已存在工具（18 内置 + 用户已发布）零行为变化
  5. 发布前全量回归：内置工具逐个「进入 → 改状态 → 退出 → 重进」验证记忆 + 新 API 测试工具


---

## 四、中期规划（1-3 个月）

1. **审核体系落地**（见第六节专项）→ 管理后台 + 举报流 + 自动扫描升级。
2. **评论系统完善**：排序、点赞、回复、@、图片；作者回复标记。
3. **热门榜单**：日/周/总榜（浏览量+点赞+收藏加权），防刷。
4. **消息通知**：收藏/评论/关注/审核结果（站内 + 邮件）。
5. **内置 AI 生成（P1）** ✅ 已完成（v1.8.0，2026-08-08）：创作页 AI 对话框，输入需求 → 流式生成 HTML → 直接进入编辑器；系统提示词强制沙盒合规白名单。v1.8.1 升级为对话式创作 + 多版本切换。
6. **模板库**：20 个高频场景模板（纪念日/记账/习惯/抽签/倒计时/计算器…），一键生成 + 改编。
7. **能力契约 + AST 静态检测（P2）**：⏸ 待定（2026-08-09 用户持保留态度）。其中「作者/用户可见的权限透明反馈」提前到 v2.0；真正的 wewoo.* 能力开放 API 暂缓，等真实需求与形态清晰再评估。
8. **微信生态**：分享卡片优化、复制文案、微信内引导升级。

## 五、长期规划（3-6 个月+）

1. **L2 受控网络代理（/api/proxy）**：白名单、限流、审计、缓存。
2. **L3 外部模式**：用户同意 + 风险提示后新标签页全权限运行；可发布"需要完整权限"的工具，允许自定义封面。
3. **开放平台**：第三方开发者注册、SDK 文档（wewoo.* API）、应用市场页、收益/激励。
4. **推荐算法**：基于分类/使用历史/点赞的个性化推荐。
5. **多端**：iOS/Android 壳（PWA 已覆盖 90%），小程序版。
6. **i18n**：英文版，海外分发。
7. **数据分析面板**：访问/使用/留存统计（创作者后台）。
8. **OAuth 第三方登录**：GitHub / Google / 微信扫码。

---

## 六、审核机制专项

目标：平台可信、合规、防滥用，同时不拖慢创作者发布节奏。

### 6.1 自动审核（秒级，发布时）
- 静态扫描升级：dangerous API 调用分类（网络/存储/弹窗/跳转/键盘记录类），给出风险等级与建议权限级别。
- 封面/标题/描述文本过滤：敏感词库、垃圾信息检测。
- 大小与复杂度限制：单工具 ≤ 1MB（防滥用）。
- 规则库可配置（Supabase 表或 JSON 配置）。

### 6.2 人工审核（分钟~小时级）
- 管理后台审核队列：新工具（尤其 L2/L3 权限）进入待审；L0 工具自动放行 + 抽检。
- 审核操作：通过 / 下架 / 退回（附原因）/ 删除 + 处罚作者。
- 审计日志：所有审核动作留痕。

### 6.3 举报与申诉
- 工具页"举报"按钮（原因：垃圾/侵权/违法/色情/其他）。
- 举报流：自动聚合 → 管理后台处理 → 结果通知举报人与作者。
- 作者申诉通道（联系管理员 / 邮件）。

### 6.4 作者信誉体系
- 作者等级：新作者 → 普通 → 可信（发布数 + 好评 + 无违规）。
- 违规积分：下架/被举报成立扣分，达阈值限权或封禁。
- 可信作者可跳过部分人工审核。

### 6.5 工具可见性状态机
草稿 → 待审核 → 公开 / 未列出 / 私密 / 已下架 / 已删除。
（发布后立即可用 + 后台异步复核的模式，可让新作者先体验，风险作者强制人工审核。）

---

## 七、性能优化专项

| 优化 | 预期收益 | 优先级 |
|------|----------|--------|
| 首页列表字段瘦身（不拉 code） | 网络体积 -80%+，首屏显著变快 | P0 |
| 缓存优先渲染（localStorage + TTL） | Supabase 慢时首屏 <1s | P0 |
| 请求并行化（首页/工具页） | 瀑布缩短 30-50% | P1 |
| 数据层拆分（data.ts → 多文件） | 构建/热更速度、可维护性 | P1 |
| Supabase preconnect + 长连接 | 连接建立耗时下降 | P2 |
| 封面图 WebP + 尺寸分级 | 图片流量下降 | P2 |
| 工具 iframe 预加载（hover/进入前） | 详情页打开更快 | P2 |
| 首页 SSR/ISR 化（服务端预取卡片） | 首屏无需等待 JS 数据请求 | P2 |

---


### 7.x 容量应对（2026-08-13 评估，运营增长配套）

> 现状：Vercel Hobby + Supabase Free。当前用量极小（17 工具 / 16 用户 / 435 使用记录）。
> 量级判断：冷启动（DAU <100）零改动；增长期（DAU 500、月 PV 10-15 万）需先做以下 3 项；爆款（单视频 10w+ 播放）依赖 1、2 项就位。

| 优先级 | 任务 | 改动点 | 触发/验收 |
|--------|------|--------|-----------|
| P0（v1.16 前） | 封面图缓存头 | 封面上传/已有对象设置 `cache-control: public, max-age=86400`（URL 已带 ?v= 版本参数，更新时自动失效）；用户上传封面压缩为 WebP（当前 PNG 148KB → ~30KB） | 首页封面请求量 -90%，带宽 -70% |
| P0（v1.16 前） | 首页数据服务端缓存/ISR | fetchTools 数据改为服务端接口 + 60s ISR（revalidate），客户端不再直接全表打 Supabase；保留 localStorage 兜底 | 爆款瞬间几千并发不再打爆 Supabase 连接池 |
| P1 | 首页列表 limit + 分页 | fetchTools 加 limit（如 200）+ 滚动加载；工具 >200 时自动启用 | 工具量增长后首页不卡 |
| P1 | usage_history 归档清理 | 定期清理 >90 天旧记录（或仅保留统计摘要），防 Free 版 500MB 撑爆 | 数据库容量长期可控 |
| P2 | 升级触发线 | Supabase 带宽 >5GB/月 或 DB >500MB → Supabase Pro（$25/月）；Vercel 带宽 >100GB/月 → Vercel Pro（$20/月） | 容量监控 + 提前 2 周预警 |

## 八、版本管理规范（强制执行）

1. 语义化版本：修复 bug → patch（1.x.x）；新功能 → minor（1.x.0）；破坏性/重构 → major。
1.5 涉及沙盒 / 注入 / 权限体系等**内核级改动一律放 major**（如 v2.0），不与 minor 混排；纯 UI / 功能 / 修复可正常走 minor / patch。
2. 每次发布前必须：更新 version.json（version + buildDate + buildTimestamp + description）、归档 CHANGELOG.md、更新 README 版本 badge、本文件勾选完成项。
3. 提交信息规范：fix: / feat: / tweak: / chore: 前缀。
4. 发布 = push 到 GitHub main（Vercel 自动部署）；部署后人工验证主页版本号与关键页面。
5. 每个里程碑发布后同步更新 AI_HANDOFF.md（架构、密钥、已知问题）。

---

## 九、下一步（本周）

1. v1.6.0~v1.6.6 已完成并推送（2026-08-06/07/08）：记忆机制、全屏/交互修复、教程/提示词适配记忆、默认示例升级 + 通用创作要求、18 个内置工具全面升级、内置工具云端持久化与最近使用/使用记录修复、全屏滚动穿透修复。
2. v1.7.0 体验与性能已完成并推送（2026-08-08）：首页缓存优先、请求并行化、服务端搜索、发布封面自定义、体验细节优化。v1.7.1（2026-08-08）修复：首页「最近使用」加载慢（缓存优先）、首页浏览量查询 400（内置工具 id 混查 UUID）。v1.7.2（2026-08-08）品牌资产落地：WewooLogo 组件、导航/页脚/登录页 Logo、favicon/PWA 图标/OG 图品牌化、引导页与空状态点缀。下一步：v1.8 内置 AI 生成（用户已备 DeepSeek API）。
3. 每完成一个版本，向用户汇报 + 推送 GitHub。
4. v1.11.0 教程入口补全 + 创作页 UI 整理已完成并推送（2026-08-09）：创作页顶部「教程」按钮（移动端图标 / 桌面端文字）、对话空状态教程引导、首页教程横幅登录后重显。下一步按 11.3 推进 P2 设计系统深化（token 完整落地 → 组件化 → 暗色模式）。
5. v1.15.6 封面截图生产 500 + 渐变封面 emoji 服务端渲染已完成并推送（2026-08-13）：@sparticuz/chromium externalize + bin/fonts 打进函数包；新增服务端渐变封面接口（iOS emoji 修复）；Vercel 无头环境字体注入（FONTCONFIG_PATH 目录 + 解压后重写 fonts.conf）。下一步：v1.15.7 修复上传图片封面失败（等用户报错细节）→ v1.16 微信分享卡片/复制文案优化 + 场景模板库 → v2.0 权限体系（wewoo.ai + wewoo.fetch，本文件 v2.0 章节已细化）。
6. 运营与增长计划已写入第十二章（2026-08-13）：渠道优先级（小红书/抖音/视频号）、内容策略、冷启动时间线、KPI、风险对策。本周动作：注册 3 平台账号 + 首批 3-5 条内置工具演示视频。
7. 容量评估与注册机制评估已完成（2026-08-13）：封面缓存头 + 首页 ISR + usage_history 归档为 P0/P1 容量项（见 7.x）；注册机制现阶段不改，企业主体后上微信/手机登录（见 12.8）。


---

## 十、上架合规准备（App Store / 国内安卓 / Google Play）

> 目标：未来迁移 iOS/Android App 时审核不返工。分两类：**早期就要布局**（影响产品功能或架构，现在不做上架时很痛，随 v1.8~v2 陆续推进）与**上架前再做**（临近上架再排期，先留计划）。

### 10.1 早期就要布局（P0，随 v1.8~v2 陆续做）

| 项 | 说明 | 对应要求 |
|----|------|---------|
| 举报 + 屏蔽 + 快速下架 | UGC 社区硬性门槛：内容审核/举报/屏蔽/下架能力。与第六章「审核机制专项」合并推进，建议把其中举报/下架部分提到近期版本 | Apple 1.2 / 5.2.5；Google UGC 政策 |
| 账号注销 + 数据导出 | App 内可注销账号并删除个人数据、可导出本人数据；现在 Supabase 邮箱登录，注销 = 删 auth 用户 + 关联数据 | 《个保法》；Apple 5.1.1(v) |
| AI 生成内容标注 + 输出过滤 | v1.8 内置 AI 时同步做：AI 结果自动标注「AI 生成」、敏感词过滤、AI 内容可举报 | Apple 5.6 生成式 AI 条款；《生成式人工智能服务管理暂行办法》 |
| 用户协议补充 UGC 准则 + AI 披露 | terms 补充：用户发布内容授权、违规下架规则、AI 辅助生成声明；privacy 补充数据收集清单 | 全平台通用 |
| URL/路由冻结 | /tool/:id、/user/:id 结构保持稳定，作为未来 Universal Links / App Links 深链的基础，不随意改路由 | 技术债务预防 |
| 隐私数据清单 | 提前整理收集字段（邮箱、使用记录、设备信息、covers 等），为 App Privacy / Data safety 表单与第三方 SDK 清单做准备 | Apple / Google |

### 10.2 上架前再做（P2，留计划，临近上架再排期）

| 项 | 说明 |
|----|------|
| Sign in with Apple | 未来加微信/第三方登录时，Apple 强制要求同屏提供「通过 Apple 登录」 |
| 实名制 / 手机号绑定 | 国内社区类 App 可能要求手机号实名；账号体系预留「邮箱 ↔ 手机号」绑定空间 |
| IAP / 支付合规 | 未来会员/付费工具属数字内容，必须走 Apple IAP（30%）；创作者分成资金流提前设计，避免被认定「规避 IAP」（功能在 App 内解锁但结算走外部 = 红线） |
| 微信开放平台分享 | 分享卡片/小程序需在微信开放平台登记；当前先保证 OG 卡片缩略图/文案可用 |
| 推送 APNs / FCM | PWA 阶段可先上 Web Push；App 化时补原生推送通道 |
| 小游戏版号策略 | 「小游戏」分类若被审核认定为游戏，中国区需游戏版号；对外定位为「工具/互动应用」，避免平台化宣传 |
| L3 外部模式上架策略 | 上架版本默认关闭或仅限受信任工具开放「新标签页全权限运行」，显式同意 + 风险提示，审核更稳妥 |
| 国内安卓资质 | 公司主体 + 软著 + APP 备案（工信部强制）+ 可能的安全评估；iOS 个人开发者账号不受此限 |
| WebView 壳审核风险 | 纯网页壳易被 Apple 4.2「最低功能」拒；App 化需带原生能力（推送/分享/登录），或评估 Capacitor 类方案 |

---

## 十一、品牌与视觉升级规划

> 背景：外部 AI 已产出「微坞 Logo 展示页」（D:\Workbuddy\2026-07-29-23-24-29\wewoo-logo-showcase，单文件 HTML + HANDOFF.md），提供完整品牌系统：主色靛蓝→紫渐变（#5046E5→#8B5CF6）、节点青 #22D3EE、墨黑 #0A1628、字体 Noto Sans SC + Inter、圆角 12/20/35px、阴影规范。当前站点无 Logo（导航/页脚仅文字），favicon 为默认图标，品牌识别为零。此章节拆为「先落地品牌资产」与「后做全站视觉升级」两阶段，视觉大升级刻意排在 v1.8 内置 AI 之后。

### 11.1 品牌资产落地（A 阶段）✅ 已完成（2026-08-08，v1.7.2）

| 项 | 说明 |
|----|------|
| WewooLogo SVG 组件 | 新建共享组件（渐变版 + 单色版两个 symbol），复用交付的 SVG 几何 |
| 导航 / 页脚替换文字 Logo | Navbar、Footer 的「微坞」文字前加图标，点击回首页 |
| favicon + manifest 图标 | 用单色版（小尺寸下渐变发糊），含 apple-touch-icon |
| OG 分享图 | 首页/工具页分享卡片加品牌标志（可先简单合成，不依赖图像 AI） |
| 引导页 / 登录页 / 空状态点缀 | 首屏/新手引导放品牌标志，增强记忆点 |

### 11.2 全站视觉大升级（B 阶段，v1.9，排在 v1.8 内置 AI 之后）

| 项 | 说明 |
|----|------|
| 设计系统 token 化 | 主色/渐变/青色点缀/字体/圆角/阴影抽成 Tailwind theme（CSS 变量） |
| 页面改版 | 首页与工具卡片、创作页、工具详情页按品牌规范统一 |
| 默认封面样式更新 | 工具默认封面改用品牌渐变 + W 元素 |
| 新 OG 图与插画 | 启用图像能力生成：OG 图、引导页插画、空状态插画、内置工具封面 |
| 暗色模式 | 依赖设计 token 水到渠成，排在视觉升级之后 |

> 为什么大升级放 v1.8 之后：① v1.8 会在创作页新增 AI 对话框等 UI，先改视觉会重复适配；② 全站视觉改动回归风险最高，应单独一版集中验证，不与功能混排；③ 视觉升级是图像能力收益最大的场景（OG/封面/插画），届时一并启用。

### 11.3 UI 迭代计划（P0-P5 分级，2026-08-09 补录）

> 按「成本低收益高 → 功能型」排序，每档对应建议版本。

#### P0 · 体验小债（v1.9.10 ✅ 已完成 2026-08-09）
| 任务 | 说明 | 状态 |
|------|------|------|
| 他人主页头像/标题显示错乱（B3） | 头像取错邮箱、标题显示 UUID | ✅ v1.9.10 注册昵称 + 主页兜底 |
| 创作页停留 30s 自动展开 AI 提示词（B10） | 改成不打扰（默认折叠，仅点击展开） | ✅ v1.9.10 确认 |
| 分类 emoji 映射统一抽常量（B7） | 3 处重复定义，防改漏 | ✅ v1.9.10 lib/constants |

#### P1 · 纯视觉统一（v1.10.0，UI 集中版）
| 任务 | 说明 | 状态 |
|------|------|------|
| 登录/注册页、引导页、用户主页按品牌规范统一 | 对齐品牌渐变 #5046E5→#8B5CF6、节点青、圆角/阴影 token | ✅ v1.10.0（登录/注册 + 用户主页） |
| 发布成功弹窗、空状态、评论/评价区统一样式 | 统一弹窗/空状态/评论卡片视觉 | ✅ v1.10.0（发布弹窗/成功卡片） |
| 交互动效体系化 | 卡片 hover、按钮按压、弹窗进出场、页面过渡 | ✅ v1.10.0（hover/按压/弹窗） |
| 顺手清掉各页硬编码圆角/颜色 | 为 token 化铺路 | ✅ v1.10.0（首轮清理） |

#### P2 · 设计系统深化（v1.10.x）
| 任务 | 说明 | 状态 |
|------|------|------|
| token 完整落地 | 青色点缀 #22D3EE、圆角 12/20/35、区块间距、页面宽度 | ✅ v1.12.0（圆角/文字/背景/宽度/青色 token + 语义工具类） |
| 组件化 | 按钮/徽章/卡片/弹窗统一组件，消除散落样式 | ✅ v1.12.0（Modal 6 处弹窗 + Badge 徽章 + panel/btn-* 工具类）；✅ v1.12.2 按钮样式经 btn-* 工具类全站统一（圆角/高度/字号） |
| 暗色模式 | 依赖 token 完成，水到渠成 | ✅ v1.12.1（深/浅切换 + 全站适配） |

#### P3 · 性能类体验（穿插在以上版本）
| 任务 | 说明 | 状态 |
|------|------|------|
| 首页列表字段瘦身 | 不拉完整 HTML code，网络体积 -80% | ✅ v1.5.0（fetchTools 只取卡片字段）+ v1.7.x 缓存优先 |
| 封面图 WebP + 尺寸分级 | 减少图片体积 | ✅ v1.13.0（18 个内置封面转 WebP，体积 -72%；next/image 自动多尺寸） |
| 工具 iframe 预加载、首页 SSR/ISR | 提升打开速度 | ✅ v1.13.0 详情数据缓存秒开（fetchToolById 写缓存 + 详情页预填，24h TTL）；首页 SSR/ISR 待后续评估 |

#### P4 · 功能型 UI（v1.11+，等产品形态稳定）
| 任务 | 说明 | 状态 |
|------|------|------|
| 场景模板库 | 创作页模板选择器（已有 6 个示例提示词一键复制，模板选择器待做） | ⏳ 待做 |
| 热门榜单 | 日/周/总榜 | ⏳ 待做 |
| 评论系统完善 | 排序/回复/作者标记 | ✅ v1.14.0（最新/最热排序、回复、作者标记、头像；@ 与图片待后续） |
| 消息通知中心 | 收藏/评论/关注/审核结果 | ⏳ 待做 |
| 个性化首页 | 收藏/推荐分区 | ⏳ 待做 |
| 工具能力徽章 | v2.0 权限透明反馈的一部分 | ⏳ 随 v2.0 |
| 微信分享卡片/复制文案优化 | 分享体验 | ⏳ 待做 |

#### P5 · 依赖图像能力（以后再说）
| 任务 | 说明 | 状态 |
|------|------|------|
| OG 图 + 空状态/引导/404 品牌插画 | 需图像能力 | ⏳ 待做 |
| 内置工具封面升级 | 需图像能力 | ⏳ 待做 |

> 依赖关系：暗色模式必须排在 P2 token 化之后；插画类排在图像能力之后；能力徽章跟着 v2.0 内核走，不单独抢。
> 建议节奏：P0 已清 → v1.10.0 做 P1 视觉统一（低风险、用户可感知）→ P4 功能型 UI 等需求更明确时再细化拆解。

## 十二、运营与增长计划（2026-08-13 新增）

> 技术线负责「做出来」，运营线负责「被用起来」。本计划与版本节奏绑定：运营动作不等待产品完美，但**内容安全底线（举报 + 下架）必须在投流之前就绪**。

### 12.1 推广前置条件（什么时候可以开始推）

| 门槛 | 状态 |
|------|------|
| 网站稳定可访问、注册登录正常、工具可用、分享链接可用 | ✅ 已满足（v1.15.6） |
| 分享卡片/复制文案优化（社交传播基础设施） | ⏳ v1.16（与 v2.0-D wewoo.share 合并） |
| 基本举报入口 + 发布内容敏感词拦截底线 | ⏳ v1.16 前补（AI 生成侧已有敏感词，发布侧补齐） |
| 未备案域名（we-woo.net 海外托管）：微信/抖音内链接可能被拦截或风险提示 | ⚠️ 冷启动期以「展示 + 引导搜索品牌词」为主，不硬塞外链；中期评估国内域名 + 备案 + 国内托管（见第十节） |

### 12.2 渠道优先级与账号矩阵

| 渠道 | 定位 | 内容形式 | 优先级 | 开号时机 |
|------|------|----------|--------|----------|
| 小红书 | 生活/效率工具演示，女性用户多，契合纪念日/记账/生活类工具 | 图文 + 15-30s 竖屏短视频 | 高 | 立即（本周） |
| 抖音 | 泛流量，趣味小游戏/实用工具 | 15-30s 竖屏短视频 | 高 | 立即（本周） |
| 微信视频号 + 公众号 | 私域触达父母辈，朋友圈传播 | 视频 + 图文 | 中 | v1.16 分享就绪后 |
| B 站 | 创作向教学（「不会编程也能做小工具」= 平台差异化卖点） | 3-5 分钟教程 | 中 | v1.17 |
| 知乎 | SEO 文字向（「有哪些实用小工具网站」类回答） | 图文长文 | 低 | 后续 |

统一账号名「微坞 WeWoo」+ Logo 头像 + 简介一句话（「AI 小工具分享社区，像发朋友圈一样分享你做的工具」）。

### 12.3 内容策略（四种内容类型）

| 类型 | 说明 | 示例选题 |
|------|------|----------|
| 工具演示 | 直接展示站内工具效果，天然适合短视频 | 2048 挑战、单位换算大全、纪念日记录器、旅行分账 |
| 创作教学 | 展示「用 AI 一句话生成小工具」，平台核心差异化 | 「让爸妈 5 分钟做出自己的小工具」 |
| 用户案例 | 转发真实用户做的工具，激励创作者 | 「网友用微坞做了个家庭药箱提醒」 |
| 话题运营 | 借势节点批量产出工具清单 | 开学季学习工具、考试倒计时、节日纪念日 |

### 12.4 冷启动时间线

| 时间 | 动作 |
|------|------|
| 第 1 周（本周） | 注册小红书/抖音/视频号（统一品牌名）；制作首批 3-5 条内置工具演示视频（用现有工具即可，无需等版本） |
| 第 2-4 周 | 隔日更，测试内容方向；用数据（播放/互动/转粉）筛出 1-2 个方向加码 |
| 第 1-2 月 | 微信生态启动（视频号 + 公众号）；分享卡片就绪后内容带「微坞」品牌引导 |
| 第 2-3 月 | 达人合作（科技/效率类博主，置换或小额）；发起「用 AI 做工具」话题活动 |
| 审核就绪（v2.0）后 | 投流 + 规模化推广（届时举报/下架/审核体系已上线） |

### 12.5 增长指标（KPI）

| 阶段 | 指标 |
|------|------|
| 冷启动期（第 1 月） | 各平台合计粉丝 1000+；单条视频最高播放 1w+；网站注册 200-500 |
| 增长期（第 2-3 月） | 月注册 1000+；DAU 500+；工具分享率 > 5% |
| 内容追踪 | 播放 → 主页点击率、视频 → 注册转化（短链/二维码区分渠道） |

### 12.6 关键风险与对策

| 风险 | 对策 |
|------|------|
| 抖音/小红书站外引流限流 | 主页只挂品牌词，内容展示但不硬塞链接，引导搜索「微坞」 |
| 微信拦截未备案域名 | 冷启动期微信内用二维码/口令，不做直接跳转；中期评估国内域名 + 备案 |
| 内容安全 | 举报 + 下架必须早于投流；发布侧敏感词底线 v1.16 前补齐 |
| 国内访问速度（Vercel 海外节点） | 推广前评估国内 CDN / 备案托管，避免转化流失 |
| 版权 | 演示素材全用站内工具，无版权风险；达人合作签口头/书面授权 |

### 12.8 注册登录演进（2026-08-13 评估）

> 现状：邮箱 + 密码注册即登录（Supabase mailer_autoconfirm=true，无需邮件确认）；无手机号/微信登录。
> 结论：**现阶段不改**——微信登录需开放平台企业主体、手机短信需企业实名（现配置 Twilio 国内不可用），等有营业执照后再做。

| 阶段 | 动作 | 前置条件 |
|------|------|----------|
| 现在（零成本） | 注册表单简化（昵称选填、密码强度提示）；游客体验最大化（浏览/用工具免登录，登录只挡发布/收藏/云记忆） | 无 |
| 有企业主体后（v2.0 前后） | 微信扫码登录（开放平台网站应用）+ 手机号验证码（腾讯云/阿里云短信，替换 Twilio） | 营业执照 + 微信开放平台认证（300 元/年）+ 短信签名备案 |
| 长期 | 游客匿名体验（匿名用户自动生成 ID，登录后合并数据） | 数据合并策略设计 |

### 12.7 里程碑（与版本绑定）

| 时间 | 版本 | 运营动作 |
|------|------|----------|
| 本周 | v1.15.7 | 注册 3 平台账号、首批 5 条演示视频 |
| v1.16 | 分享就绪 | 正式日更；微信视频号 + 公众号启动 |
| v1.17 | 模板库 | 达人合作；首个话题活动 |
| v1.18 | 榜单/通知 | 创作者激励（周榜）；用户案例征集 |
| v2.0 | 权限/审核就绪 | 投流；规模化推广 |
